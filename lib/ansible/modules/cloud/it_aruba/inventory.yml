- hosts: localhost
  connection: local
  gather_facts: no
  vars:
    arubau: "{{ lookup('ini', 'user   section='+acct+'  '+secrets) }}"
    arubap: "{{ lookup('ini', 'passwd section='+acct+'  '+secrets) }}"
    secrets: file=~/.ssh/aruba/ansible.ini
    acct: acct0
  tasks:
    - it_aruba_smart:
        user: "{{ arubau }}"
        password: "{{ arubap }}"
        dc: 1
      register:  r1

    - debug: msg="DC{{ item.DC }} ID:{{ item.id }}"
      loop: "{{ r1.srv }}"
