- hosts: localhost
  connection: local
  gather_facts: no
  tasks:
    - it_aruba_smart:
        user:     "{{ lookup('file', '~/.ssh/aruba/my.user') }}"
        password: "{{ lookup('file', '~/.ssh/aruba/my.password') }}"
      register:  re1

    - debug: var=re1
